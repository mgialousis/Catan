# Latest-change review and publication audit — 2026-09-14

Reviewed source at `f500307` and the preceding hosted deployment/TLS changes, plus
the local fixes described below. The starting worktree was clean. At the initial audit the repository was **private** and no services or hosted records had been changed.
The user subsequently authorized publication, deployment, migration-history repair and Android APK
automation. See the execution record in [Phase 7 evidence](phase-7-verification.md#reviewed-deployment-and-history-repair--2026-09-14).

## Findings and fixes

1. **Latest fixes are not deployed.** Render reports both services live at
   `72d76f4`, with automatic deployment disabled. The two-root certificate bundle
   in `4f26afd` and invitation/resource UI changes in `f500307` are absent from
   the hosted release. Publish a reviewed commit and deploy explicitly between
   games, then repeat preflight.
2. **Migration history is not aligned.** Hosted MCP `list_migrations` returns
   version `20260913200406`, name `20260909000100_foundation`; the checked-in
   file has version `20260909000100`. The previous claim that matching names
   align history was incorrect. Verify applied SQL/schema equivalence, reconcile
   tracking records, and inspect a database-push dry run before any subsequent
   schema deployment. Do not reapply the foundation to the populated schema.
   [Supabase migration troubleshooting](https://supabase.com/docs/guides/deployment/database-migrations#diagnosing-and-fixing-sync-errors)
3. **Unverified clipboard writes still announced success. Fixed locally.**
   The unknown outcome now says the copy could not be confirmed and points to
   the selectable link. Three widget cases cover confirmed, refused, and
   unverifiable copies. These simulate platform responses; they do not establish
   actual clipboard permission behavior on physical browsers.
4. **Year of Plenty omitted holdings. Fixed locally.** Its picker now receives
   the player's holdings separately from selection limits. Existing exact-choice
   coverage verifies the holdings labels too. A new trade test verifies a resource
   cannot appear on both sides and becomes selectable again when removed.
5. **Deployment instructions contradicted live state and referenced a removed
   certificate file. Fixed locally.** README, PLAN, deployment instructions and
   Phase 7 evidence now distinguish live state from current source and record
   the migration mismatch.

## Verification

| Check | Result |
| --- | --- |
| Strict TypeScript build and `npm test` | 219 passed |
| Local PostgreSQL/Auth/Socket.IO integration suite | 56 passed; all six checked application table counts unchanged |
| Flutter suite after UI fixes | 269 passed, including modal layout cases and four new widget tests |
| Final Flutter analysis | No issues |
| Flutter web release | Built successfully using local public configuration; packaging evidence only |
| API Docker image | Built `island-table-api:review`, including `supabase-roots.crt` |
| New API image smoke against local Supabase | Non-root UID 1000, database readiness and authenticated Socket.IO passed |
| Hosted preflight | Readiness, protocol/rules compatibility, static cache policy and unauthenticated socket rejection passed |
| Hosted warm version endpoint, 10 requests | Median 62 ms, p95 167 ms; not gameplay latency |
| Diff whitespace check | Passed |

Web build warnings concern the dependency's optional WebAssembly dry run and
the existing optional Cupertino icon font. The JavaScript release build succeeded.
Hosted preflight created no guest or game. Local tests/container smoke use
synthetic guest identities; their application-fixture checks are documented above.

Supabase security advisors returned one warning: leaked-password protection is
disabled. The app currently uses anonymous sign-in, so this does not establish a
password exposure in its current flow. Revisit if password accounts are introduced.
[Advisor remediation](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection)

No new physical-device, Safari, mixed-network match, hosted restart/recovery,
ingress trust, two-hour soak or backup/restore evidence was collected. Phase 7
remains open.

## Public repository audit

- Fetched remote refs. The repository has eight reachable commits and one branch;
  both local `main` and `origin/main` were audited.
- Gitleaks 8.30.1, downloaded from its official GitHub release and checked against
  the published SHA-256, found no leaks in all reachable history or a snapshot of
  current tracked files plus the new widget test. Inline allow comments were ignored.
- A separate exact-value scan checked six sensitive configuration values from
  ignored local environment/signing configuration against all 277 historical
  blobs, commit objects, and current tracked files. No matches were found.
- `.env`, operator configuration, Android signing files and real client config
  are ignored. Their sensitive files do not appear in the tracked path history.
- Inspected all 12 current tracked screenshots. They show synthetic local/practice
  sessions, including test lobby invitation codes; no credentials were visible.
- GitHub reports no releases and no Actions artifacts.

No credential leak was found. This is evidence from automated detection and a
targeted review, not a guarantee that every possible secret format is detectable.
The audit does not cover provider secrets, external Claude sessions or unpublished
local files, which are outside the proposed Git repository publication.

Publishing the existing history exposes the author's name/email, local filesystem
paths, Claude session links, and public deployment identifiers. The service URLs,
Supabase project reference and public CA certificates are not authentication
secrets. Decide whether the personal metadata/session links are acceptable first;
editing only the latest files does not remove earlier commit content. No history
rewrite or visibility change was performed. There is also no project LICENSE file;
choose the intended license if publishing as an open-source project.

## Recommended order

1. Decide whether to retain the personal metadata and session links, then publish
   the repository if desired. Keep secret scanning enabled for future changes.
2. Commit and deploy the reviewed UI/TLS fixes to both existing Render services
   between games; rerun hosted preflight.
3. Reconcile migration history before the next database change.
4. Measure trusted ingress and exercise a controlled hosted restart/recovery.
5. Complete timed and untimed matches on real phones across separate networks,
   followed by soak measurement and an isolated backup/restore rehearsal.

## Execution update

Items 2 and 3 are complete: `85326a0` is live on both Render services, hosted preflight passed,
and remote migration identity is now `20260909000100 / foundation`, with SQL/schema equivalence
verified and stored SQL preserved. The paused three-player game survived deployment unchanged
apart from the expected recovery version/log increment. The repository is now public at the
user's request, with secret scanning and push protection enabled. Signed Android APK automation
was added in `671bd48`; see [Android builds](android-builds.md) for downloads and configuration.
The initial findings above describe the state before these actions.
